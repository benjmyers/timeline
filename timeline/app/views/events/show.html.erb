<div class="container-fluid">
    <div class="modal-header">
      <p><h3> <%= @event.title %> </h3></p>
  </div>
  <div class="row-fluid showevent">
    <div class="span4">
      <p>
        <%= image_tag @event.image.url(:large) %>
      </p>
      <%= link_to 'Edit', edit_event_path(@event) %> |
      <%= link_to "Delete", @event, method: :delete, data: { confirm: "Are you sure you want to delete this   @event?"} %> | 
      <%= link_to 'Back', timeline_object_path(@event.timeline_object_id) %>
    </div>
    <div class="span8">
      <p>
        <b>Date:</b>
        <%= @event.date %>
      </p>
      <p>
        <b>Time:</b>
        <%= @event.time %>
      </p>
      <p>
        <b>Description:</b>
        <%= @event.description %>
      </p>
    </div>
  </div>
</div>